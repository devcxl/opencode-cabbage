---
name: "tdd-p0-adr"
depends_on: []
labels: ["docs"]
worktree_root: ".worktree/tdd-p0-adr/"
expected_files:
  - "docs/adr/2026-07-20-tdd-enforcement-architecture.md"
test_commands:
  - "ls docs/adr/2026-07-20-tdd-enforcement-architecture.md"
acceptance: |-
  - [x] ADR 审查通过
  - [x] 确认 Task-local PR Gate 设计
  - [x] 确认三层约束模型 (Advisory/Runtime/Repository Quality)
  - [x] 确认单 Orchestrator 约束
---

## 目标

创建 ADR 0008，确认 TDD 强制约束架构的三个核心决策：

1. **Task-local PR Gate**：新增 `canMergeTaskPR()` 独立于全局 `canMerge()`
2. **三层约束模型**：Advisory → Runtime Enforced → Repository Quality Enforced，每层有明确能力边界
3. **单 Orchestrator 约束**：v2 仅支持每个 FlowRun 一个活跃 orchestrator 进程

## 实现要点

1. 在 `docs/adr/` 下创建 `2026-07-20-tdd-enforcement-architecture.md`
2. 参考已有 ADR 格式（ADR 0001、0006、0007）
3. 记录备选方案及拒绝理由
4. 明确正向后果、风险和验证要求

## 验收标准

- [x] ADR 文件已创建并审查通过
- [x] Task-local PR Gate 与全局 `canMerge()` 分离设计已确认
- [x] 三层约束模型已定义，每层证明边界清晰
- [x] 单 Orchestrator 约束已声明
- [x] 验证要求包含"两个有依赖 Task 的全链路测试"场景

## Worktree

- 路径: `.worktree/tdd-p0-adr/`
- 分支: `feat/tdd-p0-adr`

> **状态：已完成** — ADR 0008 已创建于 `docs/adr/2026-07-20-tdd-enforcement-architecture.md`
